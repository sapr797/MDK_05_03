# Контрольная работа 3.2: Покрытие кода, тестирование функций, файловый ввод-вывод, классы и объекты, структуры данных

**Дисциплина:** МДК 05.03 Тестирование информационных систем  
**Семестр:** 6  
**Темы:** 3.6 – 3.9  
**Тип занятия:** Контрольная работа (2 часа)

---

## Цель контрольной работы

Проверить и оценить знания, умения и навыки, полученные при изучении тем 3.6–3.9:

- Покрытие кода и тестирование функций (чистые функции, функции с побочными эффектами)
- Тестирование файлового ввода-вывода (pytest.raises, monkeypatch, tmp_path)
- Тестирование классов и объектов (конструкторы, состояние, инварианты)
- Тестирование иерархий и полиморфизма (LSP, параметризация по классам)
- Тестирование структур данных (списки, словари, множества)

---

## Структура контрольной работы

Контрольная работа состоит из **трёх уровней сложности**:

| Уровень | Варианты | Максимальный балл | Оценка |
|:---|:---|:---|:---|
| **Базовый (варианты 1-8)** | 1–8 | 30 баллов | 3 (удовлетворительно) |
| **Средний (варианты 9-17)** | 9–17 | 50 баллов | 4 (хорошо) |
| **Сложный (варианты 18-25)** | 18–25 | 80 баллов | 5 (отлично) |

Студент выбирает уровень в соответствии со своими возможностями или по указанию преподавателя.

---

# БАЗОВЫЙ УРОВЕНЬ (Варианты 1–8)

## Вариант 1

### Задание 1 (5 баллов). Покрытие кода

Что такое покрытие кода? Какие типы покрытия существуют? Какой командой в pytest можно измерить покрытие?

---

### Задание 2 (10 баллов). Тестирование функций

Дана функция:

```python
def is_palindrome(s: str) -> bool:
    """Проверяет, является ли строка палиндромом."""
    s = s.lower().replace(" ", "")
    return s == s[::-1]
Напишите 3 теста (позитивные и негативные).

Задание 3 (10 баллов). Тестирование файлового ввода-вывода
Дан код:

python
def read_and_upper(file_path: str) -> str:
    with open(file_path, 'r') as f:
        return f.read().upper()
Напишите тест, использующий tmp_path, который:

Создаёт временный файл с текстом "hello"

Вызывает функцию

Проверяет, что результат равен "HELLO"

Задание 4 (5 баллов). Структуры данных
Что выведет следующий код? Ответ объясните.

python
a = [1, 2, 2, 3]
b = set(a)
c = list(b)
print(c)
Вариант 2
Задание 1 (5 баллов). Покрытие кода
Что означает красная строка в HTML-отчёте coverage? Какой минимальный порог покрытия обычно рекомендуется?

Задание 2 (10 баллов). Тестирование функций
Дана функция:

python
def factorial(n: int) -> int:
    if n < 0:
        raise ValueError("n must be non-negative")
    if n <= 1:
        return 1
    return n * factorial(n - 1)
Напишите 3 теста: на нормальное поведение и на исключение.

Задание 3 (10 баллов). Тестирование файлового ввода-вывода
Напишите тест, который проверяет, что при попытке прочитать несуществующий файл функция read_file выбрасывает FileNotFoundError.

Задание 4 (5 баллов). Классы и объекты
Что такое инвариант класса? Приведите пример инварианта для класса BankAccount.

Вариант 3
Задание 1 (5 баллов). Тестирование функций
Чем отличается чистая функция от функции с побочными эффектами? Приведите примеры.

Задание 2 (10 баллов). Покрытие кода
Как сгенерировать HTML-отчёт о покрытии с помощью pytest? Как его открыть в браузере?

Задание 3 (10 баллов). Тестирование классов
Дан класс:

python
class Counter:
    def __init__(self):
        self._count = 0
    
    def increment(self):
        self._count += 1
    
    def get_count(self):
        return self._count
Напишите 2 теста: проверка начального состояния и проверка метода increment.

Задание 4 (5 баллов). Структуры данных
Как проверить, что элемент есть в списке? А в словаре? А во множестве?

Вариант 4
Задание 1 (5 баллов). Тестирование файлового ввода-вывода
Что делает фикстура tmp_path в pytest? Какие проблемы она решает?

Задание 2 (10 баллов). Тестирование функций
Дана функция:

python
def divide(a: float, b: float) -> float:
    if b == 0:
        raise ZeroDivisionError("Division by zero")
    return a / b
Напишите тест с pytest.raises для проверки деления на ноль.

Задание 3 (10 баллов). Покрытие кода
Зачем нужен файл .coveragerc или настройки в pyproject.toml для coverage? Какие настройки можно указать?

Задание 4 (5 баллов). Классы и объекты
Для чего нужны фикстуры-фабрики при тестировании классов?

Вариант 5
Задание 1 (5 баллов). Тестирование функций
Что такое pytest.raises? Как он используется для проверки исключений?

Задание 2 (10 баллов). Тестирование файлового ввода-вывода
Как с помощью monkeypatch эмулировать ошибку PermissionError при записи файла? Напишите пример кода.

Задание 3 (10 баллов). Тестирование классов
Дан класс:

python
class User:
    def __init__(self, name: str):
        self.name = name
        self.is_active = True
    
    def deactivate(self):
        self.is_active = False
Напишите 2 теста: проверка начального состояния и проверка метода deactivate.

Задание 4 (5 баллов). Структуры данных
Как проверить, что два списка содержат одинаковые элементы, но порядок не важен?

Вариант 6
Задание 1 (5 баллов). Покрытие кода
Что такое покрытие ветвей (branch coverage) и чем оно отличается от покрытия строк?

Задание 2 (10 баллов). Тестирование функций
Дана функция:

python
def safe_int(value: str) -> int:
    try:
        return int(value)
    except ValueError:
        return 0
Напишите 2 теста: для валидного и невалидного ввода.

Задание 3 (10 баллов). Тестирование файлового ввода-вывода
Напишите тест для функции append_to_file, который:

Создаёт временный файл с содержимым "Hello"

Добавляет строку " World"

Проверяет, что в файле "Hello World"

Задание 4 (5 баллов). Классы и объекты
Как проверить, что после выполнения метода с исключением состояние объекта не изменилось?

Вариант 7
Задание 1 (5 баллов). Тестирование функций
Что такое «негативные тестовые сценарии»? Почему они важны?

Задание 2 (10 баллов). Покрытие кода
Как запустить pytest с проверкой минимального порога покрытия (например, 80%)?

Задание 3 (10 баллов). Тестирование классов
Дан класс:

python
class Stack:
    def __init__(self):
        self._items = []
    
    def push(self, item):
        self._items.append(item)
    
    def pop(self):
        if not self._items:
            raise IndexError("pop from empty stack")
        return self._items.pop()
    
    def is_empty(self):
        return len(self._items) == 0
Напишите 3 теста: проверка пустого стека, push, pop.

Задание 4 (5 баллов). Структуры данных
Как проверить, что одно множество является подмножеством другого?

Вариант 8
Задание 1 (5 баллов). Тестирование файлового ввода-вывода
Как с помощью capsys перехватить вывод программы в stdout?

Задание 2 (10 баллов). Тестирование функций
Дана функция:

python
def max_of_list(numbers: list) -> int:
    if not numbers:
        raise ValueError("List cannot be empty")
    return max(numbers)
Напишите 2 теста: нормальное поведение и исключение.

Задание 3 (10 баллов). Покрытие кода
Какие строки обычно исключают из измерения покрытия? Приведите примеры.

Задание 4 (5 баллов). Классы и объекты
Что такое полиморфизм? Как тестировать полиморфное поведение?

СРЕДНИЙ УРОВЕНЬ (Варианты 9–17)
Вариант 9
Задание 1 (10 баллов). Покрытие кода
Дан модуль:

python
# calculator.py
def add(a, b):
    return a + b

def subtract(a, b):
    return a - b

def multiply(a, b):
    return a * b

def divide(a, b):
    if b == 0:
        raise ValueError("Cannot divide by zero")
    return a / b
Написаны тесты только для add и divide. Какое покрытие строк и ветвей? Какие строки не покрыты? Напишите недостающие тесты.

Задание 2 (15 баллов). Тестирование файлового ввода-вывода
Дан класс:

python
class ConfigManager:
    def __init__(self, config_path: str):
        self.config_path = config_path
        self.config = {}
    
    def load(self):
        try:
            with open(self.config_path, 'r') as f:
                import json
                self.config = json.load(f)
        except FileNotFoundError:
            self.config = {}
        except json.JSONDecodeError:
            raise ValueError("Invalid JSON format")
    
    def get(self, key, default=None):
        return self.config.get(key, default)
Напишите тесты с использованием tmp_path:

Загрузка существующего валидного JSON

Загрузка несуществующего файла (должен вернуть пустой словарь)

Загрузка невалидного JSON (должно быть исключение)

Задание 3 (15 баллов). Тестирование классов и объектов
Дан класс:

python
class ShoppingCart:
    def __init__(self):
        self._items = {}
    
    def add_item(self, product_id: str, quantity: int):
        if quantity <= 0:
            raise ValueError("Quantity must be positive")
        self._items[product_id] = self._items.get(product_id, 0) + quantity
    
    def remove_item(self, product_id: str):
        if product_id in self._items:
            del self._items[product_id]
    
    def get_quantity(self, product_id: str) -> int:
        return self._items.get(product_id, 0)
    
    def get_total_items(self) -> int:
        return sum(self._items.values())
    
    def clear(self):
        self._items.clear()
Напишите тесты:

Проверка начального состояния

Добавление одного товара

Добавление нескольких единиц товара

Добавление нескольких разных товаров

Удаление товара

Очистка корзины

Обработка ошибки (отрицательное количество)

Задание 4 (10 баллов). Структуры данных
Напишите функцию find_intersection(list1, list2), которая возвращает список общих элементов без дубликатов. Затем напишите параметризованные тесты для неё (минимум 3 набора данных).

Вариант 10
Задание 1 (10 баллов). Покрытие кода
Объясните, как интерпретировать HTML-отчёт coverage. Что означают зелёные, красные и жёлтые строки? Как найти непокрытые ветви?

Задание 2 (15 баллов). Тестирование файлового ввода-вывода
Дан класс:

python
class FileCopier:
    @staticmethod
    def copy(src: str, dst: str, overwrite: bool = False):
        import shutil
        import os
        
        if not os.path.exists(src):
            raise FileNotFoundError(f"Source not found: {src}")
        
        if os.path.exists(dst) and not overwrite:
            raise FileExistsError(f"Destination exists: {dst}")
        
        shutil.copy2(src, dst)
Напишите тесты с использованием tmp_path и monkeypatch:

Успешное копирование

Исходный файл не существует → исключение

Целевой файл существует и overwrite=False → исключение

Целевой файл существует и overwrite=True → успешное копирование

Эмуляция PermissionError при копировании

Задание 3 (15 баллов). Тестирование иерархий и полиморфизма
Создайте иерархию классов:

Shape (абстрактный) с методами area() и perimeter()

Circle (радиус)

Rectangle (ширина, высота)

Square (сторона, наследуется от Rectangle)

Напишите полиморфные тесты:

Базовый тестовый класс BaseShapeTest

Тесты для каждого наследника

Параметризованный тест для проверки area() для всех фигур

Задание 4 (10 баллов). Структуры данных
Дана функция:

python
def group_by_first_letter(words: List[str]) -> Dict[str, List[str]]:
    """Группирует слова по первой букве."""
    result = {}
    for word in words:
        first = word[0].lower()
        if first not in result:
            result[first] = []
        result[first].append(word)
    return result
Напишите тесты:

Пустой список

Слова с разными первыми буквами

Слова с одинаковыми первыми буквами

Проверка сохранения порядка внутри групп

Вариант 11
Задание 1 (10 баллов). Покрытие кода
Как настроить CI/CD для автоматического запуска тестов с проверкой покрытия? Приведите пример конфигурации GitHub Actions.

Задание 2 (15 баллов). Тестирование файлового ввода-вывода
Дан класс:

python
class Logger:
    def __init__(self, log_file: str):
        self.log_file = log_file
    
    def log(self, message: str, level: str = "INFO"):
        from datetime import datetime
        timestamp = datetime.now().isoformat()
        entry = f"[{timestamp}] {level}: {message}\n"
        
        with open(self.log_file, 'a') as f:
            f.write(entry)
    
    def read_logs(self) -> List[str]:
        with open(self.log_file, 'r') as f:
            return f.readlines()
Напишите тесты с использованием tmp_path и monkeypatch для мокирования времени:

Запись одного сообщения

Запись нескольких сообщений

Проверка формата временной метки (с мокированием datetime.now)

Задание 3 (15 баллов). Тестирование классов
Дан класс:

python
class BankAccount:
    def __init__(self, owner: str, initial_balance: float = 0):
        if initial_balance < 0:
            raise ValueError("Initial balance cannot be negative")
        self.owner = owner
        self.balance = initial_balance
        self.transactions = []
    
    def deposit(self, amount: float):
        if amount <= 0:
            raise ValueError("Deposit amount must be positive")
        self.balance += amount
        self.transactions.append(("deposit", amount))
    
    def withdraw(self, amount: float):
        if amount <= 0:
            raise ValueError("Withdrawal amount must be positive")
        if amount > self.balance:
            raise ValueError("Insufficient funds")
        self.balance -= amount
        self.transactions.append(("withdraw", amount))
    
    def get_transaction_count(self) -> int:
        return len(self.transactions)
Напишите полный набор тестов:

Конструктор (валидный, невалидный)

deposit (успех, ошибка)

withdraw (успех, ошибки)

Проверка истории транзакций

Инварианты (баланс не отрицательный)

Задание 4 (10 баллов). Структуры данных
Напишите функцию merge_dicts(dict1, dict2), которая объединяет два словаря. При конфликте значение из dict2 заменяет значение из dict1. Затем напишите параметризованные тесты.

Вариант 12
Задание 1 (10 баллов). Покрытие кода
Что такое мутационное тестирование? Как оно связано с покрытием кода?

Задание 2 (15 баллов). Тестирование файлового ввода-вывода
Дан код:

python
def process_csv(file_path: str) -> List[Dict]:
    import csv
    result = []
    with open(file_path, 'r') as f:
        reader = csv.DictReader(f)
        for row in reader:
            result.append(row)
    return result
Напишите тесты:

Обычный CSV-файл

Пустой CSV-файл

Отсутствие файла (исключение)

Некорректный CSV (с помощью monkeypatch для open)

Задание 3 (15 баллов). Тестирование иерархий
Создайте абстрактный класс Cache и его реализацию InMemoryCache. Напишите:

Базовый тестовый класс для всех реализаций Cache

Тесты для InMemoryCache

Тест с create_autospec для проверки контракта

Задание 4 (10 баллов). Структуры данных
Дана функция:

python
def transpose(matrix: List[List[int]]) -> List[List[int]]:
    """Транспонирует матрицу."""
    if not matrix:
        return []
    rows = len(matrix)
    cols = len(matrix[0])
    return [[matrix[i][j] for i in range(rows)] for j in range(cols)]
Напишите тесты:

Пустая матрица

Квадратная матрица

Прямоугольная матрица 2×3

Матрица 1×N

СЛОЖНЫЙ УРОВЕНЬ (Варианты 18–25)
Вариант 18
Задание 1 (15 баллов). Покрытие кода
Проанализируйте отчёт coverage (представлен ниже). Какие строки не покрыты? Напишите недостающие тесты для достижения 100% покрытия.

python
# validator.py
def validate_email(email: str) -> bool:
    if '@' not in email:  # строка 2
        return False       # строка 3
    local, domain = email.split('@')  # строка 4
    if not local:          # строка 5
        return False       # строка 6
    if '.' not in domain:  # строка 7
        return False       # строка 8
    return True            # строка 9
Отчёт coverage показывает, что строки 3, 6 и 8 зелёные (покрыты), строка 9 красная (не покрыта).

Задание 2 (20 баллов). Тестирование файлового ввода-вывода
Разработайте класс ConfigManager с методами:

load() — загрузка из JSON-файла

save() — сохранение в JSON-файл

get(key, default) — получение значения

set(key, value) — установка значения

reload() — перезагрузка из файла

Напишите полный набор тестов, включая:

Позитивные сценарии

Ошибки файловой системы (с monkeypatch)

Ошибки парсинга JSON

Тесты с tmp_path

Тесты производительности (для большого количества ключей)

Задание 3 (25 баллов). Тестирование классов и полиморфизма
Разработайте систему для расчёта скидок в интернет-магазине:

Базовый класс DiscountStrategy:

calculate(original_price: float) -> float — абстрактный метод

Конкретные стратегии:

NoDiscount — скидка 0%

PercentageDiscount(percent) — процентная скидка

FixedDiscount(amount) — фиксированная скидка

ThresholdDiscount(threshold, discount_percent) — скидка при превышении порога

SeasonalDiscount() — комбинированная скидка (20% для товаров дороже 1000)

Напишите:

Базовый тестовый класс BaseDiscountTest

Тесты для каждой стратегии

Параметризованный тест для всех стратегий

Тест для композиции стратегий

Тест фабрики, создающей стратегии по типу

Задание 4 (20 баллов). Структуры данных
Разработайте класс OrderBook для хранения и обработки заказов:

Методы:

add_order(order_id, price, quantity) — добавление заказа

remove_order(order_id) — удаление заказа

get_orders_by_price(min_price, max_price) — получить заказы в ценовом диапазоне

get_top_n_orders(n) — получить N самых дорогих заказов

get_statistics() — вернуть статистику (макс, мин, средняя цена, количество)

merge_with(other_orderbook) — объединить с другой книгой заказов

Напишите полный набор тестов:

Все методы

Граничные случаи

Производительность (10000 заказов)

Сравнение книг заказов

Вариант 19
Задание 1 (15 баллов). Покрытие кода
Дан модуль:

python
# data_processor.py
def process_list(items):
    result = []
    for i, item in enumerate(items):
        if i % 2 == 0:        # строка 3
            result.append(item * 2)  # строка 4
        else:                  # строка 5
            result.append(item * 3)  # строка 6
    return result              # строка 7
Текущие тесты:

python
def test_process_list():
    assert process_list([1, 2, 3]) == [2, 6, 6]
Какое покрытие строк и ветвей? Напишите недостающие тесты для 100% покрытия.

Задание 2 (20 баллов). Тестирование файлового ввода-вывода
Создайте класс DatabaseBackup с методами:

backup(db_path, backup_path) — создание резервной копии

restore(backup_path, db_path) — восстановление из резервной копии

verify(backup_path) — проверка целостности резервной копии (хеш-сумма)

list_backups(backup_dir) — список всех резервных копий

Напишите тесты с использованием tmp_path и monkeypatch, включая тесты на ошибки (диск заполнен, недостаточно прав, битый файл).

Задание 3 (25 баллов). Тестирование иерархий
Разработайте иерархию для отправки сообщений:

Базовый класс MessageSender:

send(message: Message) -> SendResult — абстрактный метод

get_status(message_id) -> Status — статус отправки

Конкретные реализации:

EmailSender(host, port, user, password)

SmsSender(api_key, api_secret)

TelegramSender(bot_token)

PushSender(api_key, app_id)

Вспомогательные классы:

Message (id, to, subject, body, attachments)

SendResult (success, message_id, error)

Status (pending, sent, delivered, failed)

Напишите:

Полные тесты для всех классов

Полиморфные тесты через базовый класс

Тесты фабрики MessageSenderFactory

Интеграционные тесты с мокированием внешних API

Тест на принцип подстановки Лисков (LSP)

Задание 4 (20 баллов). Структуры данных
Разработайте класс TimeSeries для работы с временными рядами:

Методы:

add(timestamp, value) — добавление точки

get_range(start, end) — получить точки в интервале

resample(frequency, aggregation) — пересэмплирование (среднее, сумма, мин, макс)

moving_average(window) — скользящее среднее

interpolate(method) — интерполяция пропущенных значений (linear, nearest)

align(other_series) — выравнивание двух рядов по времени

Напишите тесты:

Все методы

Граничные случаи (пустой ряд, одна точка)

Производительность

Сравнение с эталонными значениями

Вариант 20
Задание 1 (15 баллов). Покрытие кода
Объясните разницу между покрытием строк и покрытием ветвей. Приведите пример кода, где покрытие строк 100%, а покрытие ветвей — нет.

Задание 2 (20 баллов). Тестирование файлового ввода-вывода
Разработайте класс LogAnalyzer:

Методы:

load_log(file_path) — загрузка лог-файла

filter_by_level(level) — фильтрация по уровню (INFO, WARNING, ERROR)

filter_by_time(start, end) — фильтрация по времени

search(keyword) — поиск по ключевому слову

get_statistics() — статистика (количество по уровням, частота ключевых слов)

export(filtered_log, output_path) — экспорт в файл

Напишите тесты с использованием tmp_path и monkeypatch для мокирования времени.

Задание 3 (25 баллов). Тестирование классов и полиморфизма
Разработайте систему для обработки платежей:

Базовый класс PaymentProcessor:

process(amount, currency, payment_data) -> PaymentResult

Конкретные реализации:

CreditCardProcessor (номер, срок, CVV)

PayPalProcessor (email)

CryptoProcessor (адрес кошелька)

CashProcessor (только для офлайн)

Классы результата:

PaymentResult (success, transaction_id, error, timestamp)

RefundResult

Напишите:

Тесты для всех процессоров

Полиморфные тесты

Тесты фабрики

Тесты обработки ошибок

Тесты логирования транзакций

Задание 4 (20 баллов). Структуры данных
Разработайте класс MultiKeyDict — словарь с множественными ключами:

Методы:

add(key_group, value) — добавить значение для группы ключей

get_by_key(key) — получить все значения, связанные с ключом

get_by_key_group(key_group) — получить все значения для группы ключей

remove_key(key) — удалить ключ

remove_key_group(key_group) — удалить всю группу

find_common_keys(value) — найти все ключи, связанные с данным значением

merge(other_dict) — объединение с другим MultiKeyDict

Напишите тесты:

Все методы

Граничные случаи

Производительность

Консистентность данных

Вариант 21
(Аналогично варианту 20, но с другой предметной областью — система управления библиотекой)

Вариант 22
(Аналогично варианту 20, но с другой предметной областью — система бронирования билетов)

Вариант 23
(Аналогично варианту 20, но с другой предметной областью — система управления задачами)

Вариант 24
(Аналогично варианту 20, но с другой предметной областью — система чатов и уведомлений)

Вариант 25
(Аналогично варианту 20, но с другой предметной областью — система аналитики продаж)

Лист ответов
text
КОНТРОЛЬНАЯ РАБОТА 3.2

Студент: _________________
Группа: _________________
Дата: _________________
Вариант №: ___ Уровень: □ Базовый □ Средний □ Сложный

=== ЗАДАНИЕ 1 ===

_______________________________________________________________
_______________________________________________________________

=== ЗАДАНИЕ 2 ===

_______________________________________________________________
_______________________________________________________________

=== ЗАДАНИЕ 3 ===

_______________________________________________________________
_______________________________________________________________

=== ЗАДАНИЕ 4 ===

_______________________________________________________________
_______________________________________________________________

=== ИТОГО ===

Набрано баллов: _____ / _____
Оценка: _____

Подпись студента: _____________
Подпись преподавателя: _____________
Ключи к оценке
Уровень	Макс. балл	Оценка 3	Оценка 4	Оценка 5
Базовый	30	18–23	24–27	28–30
Средний	50	30–39	40–45	46–50
Сложный	80	48–59	60–71	72–80
Успешной сдачи контрольной работы!
